import Header from "../../features/header/Header";

const About = () => {
  const text = "Hello world!";
  return (
    <div>
      <Header />
      <p>{text}</p>
    </div>
  );
};

export default About;
