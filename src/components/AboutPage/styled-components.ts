import styled from "styled-components";
import { GradientTitle } from "../../styles/styled-components/basic";

const AboutTitle = styled(GradientTitle)`
  margin-bottom: 15px;
`;

export { AboutTitle };
